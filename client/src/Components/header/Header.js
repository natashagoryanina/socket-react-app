import React from 'react';
import Navigation from '../navigation/Navigation';

const Header = () => {
    return (
        <header>
            <h2>Header</h2>
            <Navigation/>
        </header>
    );
};

export default Header;